/**
 * 卡片吸顶
 */
import Magix from 'magix';
import * as View from '../mx-util/view';

export default View.extend({
    init(extra) {
        this.assign(extra);
    },
    assign(extra) {
        return true;
    }
});